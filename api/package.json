{
  "name": "pet-tracker-api",
  "version": "1.0.0",
  "description": "Azure Functions API for Pet Tracker",
  "main": "src/functions.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "azure-functions-core-tools": "^4.0.0"
  }
}
